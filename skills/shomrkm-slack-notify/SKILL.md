---
name: shomrkm-slack-notify
description: shomrkm の個人 Slack 通知チャンネルにメッセージを投稿する。自動化スキルの実行結果 (Project の進捗ダイジェスト、Knowledge の作成完了、エラー発生) を通知するとき、または shomrkm が「Slack に通知して」「Slack に流して」と言ったときに使う。投稿先チャンネルと書式のルールを持つ。
---

# shomrkm-slack-notify

shomrkm の個人 Slack ワークスペースの通知チャンネルへの投稿を担う。

## 責務

**このスキルがやること**: 渡された内容を、決まったチャンネルに、決まった書式で投稿する。

**このスキルがやらないこと**: 通知する中身を作ること。何を通知すべきかは呼び出し側が決める。

## 送信方法

**Slack MCP は使わない。Bot Token で Web API を直接叩く。**

理由: MCP のツール名は実行環境によって変わり (対話セッションとヘッドレス実行で別名になる)、さらにどのワークスペースに接続されるか制御できない。実際に個人ではなく別ワークスペースに繋がり `channel_not_found` で失敗した。Bot Token なら token を発行したワークスペースに必ず届く。

| 項目 | 値 |
|---|---|
| チャンネル ID | `C05C6SS2KJA` |
| Bot | `shomrkm-ai` (個人ワークスペース `shomrkm`) |
| token | 環境変数 `SLACK_BOT_TOKEN` |
| API | `https://slack.com/api/chat.postMessage` |

token は `~/.zshrc` に `export SLACK_BOT_TOKEN=xoxb-...` として置く (`chmod 600` を推奨)。`settings.json` の `env` には書かない — 設定ファイルは人に見せる機会が多く、token が混ざると漏れやすい。

対話セッションからは環境変数として見える。ただし **launchd は `.zshrc` を読まない**ため、無人実行するスクリプトでは冒頭で読み込む必要がある。

### 送信コマンド

```bash
curl -sS -X POST https://slack.com/api/chat.postMessage \
  -H "Authorization: Bearer $SLACK_BOT_TOKEN" \
  -H "Content-Type: application/json; charset=utf-8" \
  -d @- <<'JSON'
{"channel":"C05C6SS2KJA","text":"ここに本文","unfurl_links":false}
JSON
```

本文には改行やクォートが入るため、`-d @-` + ヒアドキュメントで渡す。シェルの引数に直接埋め込むとエスケープが壊れる。

### 送信後は必ず結果を確認する

**Slack API は失敗しても HTTP 200 を返す。** レスポンス JSON の `ok` を見ること。

| `error` | 意味と対処 |
|---|---|
| (`"ok":true`) | 成功 |
| `not_in_channel` | Bot が招待されていない。チャンネルで `/invite @shomrkm-ai` |
| `channel_not_found` | チャンネル ID が違うか、token が別ワークスペースのもの |
| `invalid_auth` / `token_revoked` | token が無効。再発行が必要 |
| `missing_scope` | Bot Token Scopes に `chat:write` がない |

`SLACK_BOT_TOKEN` が未設定の場合は、**エラーで落とさず「通知をスキップした」と報告して終了する**。呼び出し元の処理 (Notion の更新など) は既に完了しているため。

## 投稿ルール

1. **1回の実行 = 1投稿**。複数の対象があってもまとめて1つの投稿にする。対象ごとに投稿すると通知疲れで読まれなくなる
2. **プレーンテキスト + 絵文字**を使う。Block Kit は使わない (個人チャンネルへの単方向通知でインタラクションが不要なため)
3. **要対応のものを先頭に置く**。正常なものは畳んで短く書く。均等に並べると注意を向けるべき対象が埋もれる
4. **詳細は書かない**。Slack は「拾うべきものを見つける索引」として使い、詳細は Notion 側に置いてリンクする
5. **通知する内容が無いときは投稿しない**。空の通知は読まれなくなる原因になる

## 書式

呼び出し側の用途に応じて選ぶ。いずれも1投稿に収める。

### ダイジェスト型 (複数対象の状況報告)

```
📊 <タイトル> — <日付>

⚠️ 要対応 2件
• <対象名> — <状態> · <補足>
  └ 次の一手: <アクション>
• <対象名> — <状態> · <補足>

✅ 順調 2件
• <対象名> (<進捗>)
• <対象名> (<進捗>)

→ 詳細は <リンク先の案内>
```

### 完了報告型 (単一の成果物)

```
📚 <何をしたか> — 「<成果物名>」
   <文脈となる情報>
   → <URL>
```

### エラー型

失敗は必ず通知する。無人実行では失敗が誰にも気づかれないことが最大のリスクになる。

```
⚠️ <何に失敗したか>
   <エラーの概要>
   <部分的に成功したものがあればその件数>
```

## 避けるべき間違い

- ❌ Slack MCP のツールを使う (環境依存で別ワークスペースに飛ぶ。token 方式に統一する)
- ❌ token をスキルやスクリプトに直書きする (`$SLACK_BOT_TOKEN` を参照する)
- ❌ レスポンスの `ok` を確認せず成功とみなす (失敗しても HTTP 200 が返る)
- ❌ 対象ごとに複数投稿する (1実行1投稿)
- ❌ Notion に書いた内容を Slack にも全文コピーする (Slack は索引)
- ❌ 失敗を黙って終了する (エラーこそ通知する)
- ❌ 通知する中身が無いのに「変更なし」と投稿する (投稿しない)
- ❌ チャンネル ID を推測する (`C05C6SS2KJA` を使う)
